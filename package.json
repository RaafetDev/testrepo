{
  "name": "localtunnel-server",
  "version": "0.0.1",
  "description": "server for localtunnel",
  "main": "lib/Server.js",
  "bin": {
    "localtunnel-server": "./bin/server.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "http-proxy": "^1.18.1",
    "tldjs": "^2.3.1",
    "yargs": "^17.0.1"
  }
}
